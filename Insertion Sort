import java.util.*;
class Main{
public static v,oid main(String args[]){
int arr[] = {1,6,8,3,4};
for(int i=1;i<arr.length;i++){
int key = arr[i];
int j = arr[i] - 1;
while(arr[j] >= 0 && arr[j] > key){
arr[j+1] = arr[j];
j--;
}
arr[j+1] = key;
}
}
}
